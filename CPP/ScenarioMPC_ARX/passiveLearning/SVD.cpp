#include "../setup.h"

#ifdef FIXED
static inline fx fx_abs(fx x) {
    return (x < 0) ? -x : x;
}
#endif

// SVD decomposition for square matrix
void SVD(const theta_type** A, const int nRows, const int nCols, theta_type** U, theta_type** S, theta_type** V)
{

    return;
}
